- [ ] Update `index.html` CSS so desktop widgets use absolute positioning and show a grab cursor
- [ ] Add JS draggable logic for `.widget` elements (desktop only) and set initial positions so Calendar is visible
- [ ] Quick manual test: reload, verify Calendar is draggable and visible; verify mobile layout unchanged

